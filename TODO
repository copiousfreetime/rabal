- Add in support for Darcs (initialization, .boring, addition)
- Add in support for CVS (initialization, .cvsignore, addition)
- Add in support for SVN (initialization, svn:ignore, addition)
- Add in support for initial license declaration
- Check an existing project, see what it is missing ?
- Add in support for using Hoe
- make the directories have name spaces so others can hook into it
- allow additional libraries/modules to be loaded from other gems.
  as GemPlugin's ?
- specification for everything is based upon the Gem::Specification
- for FileTree and DirectoryTree make sure to set the name and such
  correctly

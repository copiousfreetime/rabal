require 'rubygems'
require 'rake/gempackagetask'
require 'rake/clean'
require 'rake/rdoctask'

$: << File.join(File.dirname(__FILE__),"lib")
require '<%= root.name %>'

# load all the extra tasks for the project
TASK_DIR = File.join(File.dirname(__FILE__),"tasks")
FileList[File.join(TASK_DIR,"*.rb")].each do |tasklib|
    require "tasks/#{File.basename(tasklib)}"
end

task :default => "test:default"

#-----------------------------------------------------------------------
# Documentation
#-----------------------------------------------------------------------
namespace :doc do

    # generating documentation locally
    Rake::RDocTask.new do |rdoc|
        rdoc.rdoc_dir   = <%= root.name.capitalize %>::SPEC.local_rdoc_dir
        rdoc.options    = <%= root.name.capitalize %>::SPEC.rdoc_options 
        rdoc.rdoc_files = <%= root.name.capitalize %>::SPEC.rdoc_files
    end

    desc "View the RDoc documentation locally"
    task :view => :rdoc do
        show_files <%= root.name.capitalize %>::SPEC.local_rdoc_dir
    end

end

#-----------------------------------------------------------------------
# Packaging and Distribution
#-----------------------------------------------------------------------
namespace :dist do

    Rake::GemPackageTask.new(<%= root.name.capitalize %>::SPEC) do |pkg|
        pkg.need_tar = <%= root.name.capitalize %>::SPEC.need_tar
        pkg.need_zip = <%= root.name.capitalize %>::SPEC.need_zip
    end

    desc "Install as a gem"
    task :install => [:clobber, :package] do
        sh "sudo gem install pkg/#{<%= root.name.capitalize %>::SPEC.full_name}.gem"
    end

    # uninstall the gem and all executables
    desc "Uninstall gem"
    task :uninstall do 
        sh "sudo gem uninstall #{<%= root.name.capitalize %>::SPEC.name} -x"
    end

    desc "dump gemspec"
    task :gemspec do
        puts <%= root.name.capitalize %>::SPEC.to_ruby
    end

    desc "reinstall gem"
    task :reinstall => [:install, :uninstall]

end
